[package]
name = "saps-db-embedded-pool-macro"
version = "0.1.0"
edition = "2024"
description = "Procedural macro for defining sqlx PostgreSQL connection pools backed by an embedded PostgreSQL instance"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
quote = { workspace = true }
syn = { workspace = true }
proc-macro2 = { workspace = true }
