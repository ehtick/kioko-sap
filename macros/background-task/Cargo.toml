[package]
name = "saps-background-task"
version = "0.1.0"
edition = "2024"
description = "Procedural macro for defining background task functions"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
quote = { workspace = true }
syn = { workspace = true }
proc-macro2 = { workspace = true }
